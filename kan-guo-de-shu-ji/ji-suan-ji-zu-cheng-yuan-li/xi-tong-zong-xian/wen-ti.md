```
总线结构与通信控制

目前计算机都采用什么总线结构？采用哪些通信控制？
答:同步、异步、半同步、分离式

```

```
总线通信控制---分离式通信
在分离式通信过程中：

各模块有权申请占用总线

采用同步的方式进行通信，不等对方问答

各模块准备数据时，不占用总线

总线被占用时，无空闲..

问题：在第二步中，为什么要采用同步的方式进行通信，有什么优点？

答:同步方式无需应答，节约时间
```

```
计数器定时查询的判优问题

在总线判优控制中，常见的集中控制优先权仲裁方式有3种，分别是链式查询、计数器定时查询、独立请求式，
那么在计时器定时查询中，可以采用什么样的判优方法，让每个i/o接口的请求都能得到快速的处理？
答:从上次结束的位置，循环计数
```



